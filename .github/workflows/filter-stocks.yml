name: Filter Stocks by Volume

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      debug:
        description: 'Enable debug mode'
        required: false
        default: '0'

jobs:
  filter-stocks:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run stock filtering
      id: filter
      env:
        TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API }}
        DEBUG: ${{ github.event.inputs.debug || '0' }}
        DATA_DIR: data
        OUTPUT_DIR: data/filtered
      run: |
        echo "🚀 Démarrage du filtrage des stocks"
        node scripts/stock-filter-by-volume.js
    
    - name: Upload filtered data
      uses: actions/upload-artifact@v4
      with:
        name: filtered-stocks
        path: data/filtered/**
        retention-days: 30
    
    - name: Commit filtered data
      if: success()
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add data/filtered/*.json data/filtered/*.csv || true
        git diff --staged --quiet || git commit -m "Update filtered stocks data [skip ci]"
        git push
    
    - name: Summary
      if: always()
      run: |
        echo "## 📊 Résumé du filtrage" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ -f data/filtered/stocks_us_filtered.json ]; then
          echo "- ✅ US: $(jq '.stats.total_stocks' data/filtered/stocks_us_filtered.json) stocks" >> $GITHUB_STEP_SUMMARY
        fi
        if [ -f data/filtered/stocks_europe_filtered.json ]; then
          echo "- ✅ Europe: $(jq '.stats.total_stocks' data/filtered/stocks_europe_filtered.json) stocks" >> $GITHUB_STEP_SUMMARY
        fi
        if [ -f data/filtered/stocks_asia_filtered.json ]; then
          echo "- ✅ Asie: $(jq '.stats.total_stocks' data/filtered/stocks_asia_filtered.json) stocks" >> $GITHUB_STEP_SUMMARY
        fi
