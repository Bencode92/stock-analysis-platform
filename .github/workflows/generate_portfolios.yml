name: Génération des Portefeuilles

on:
  push:
    branches: [ main ]
    paths:
      - actualites.html
      - marche.html
  schedule:
    - cron: "0 8 * * *"   # UTC
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: portfolios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate-portfolios:
    runs-on: ubuntu-latest
    env:
      API_CHAT: ${{ secrets.API_CHAT }}
      PYTHONUNBUFFERED: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          cache: "pip"

      - name: 📦 Installer les dépendances (cachées)
        run: |
          echo -e "pandas\nnumpy\nrequests\nbeautifulsoup4\nlxml" > requirements.txt
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔐 Vérifier la présence de la clé API
        run: |
          if [ -z "${API_CHAT}" ]; then
            echo "❌ Secret API_CHAT manquant dans le dépôt (Settings > Secrets and variables > Actions)."
            exit 1
          fi

      - name: 🧹 Nettoyage des placeholders éventuels
        run: |
          mkdir -p data debug/prompts
          if [ -f data/portfolio_explanations.json ] && [ "$(cat data/portfolio_explanations.json | tr -d ' \n\r')" = "1" ]; then
            echo "⚠️ Placeholder détecté dans data/portfolio_explanations.json → suppression"
            rm -f data/portfolio_explanations.json
          fi

      - name: 🚀 Exécuter le script
        run: python generate_portfolios.py

      - name: ✅ Sanity checks des sorties
        run: |
          set -e
          for f in data/portfolios.json data/portfolio_explanations.json data/portfolio_explanations.md; do
            if [ ! -s "$f" ]; then
              echo "❌ Fichier manquant ou vide: $f"; exit 1
            fi
          done
          python - << 'PY'
import json,sys
for f in ["data/portfolios.json","data/portfolio_explanations.json"]:
    with open(f,"r",encoding="utf-8") as fh:
        json.load(fh)
print("JSON OK")
PY

      - name: 💾 Commit & push
        run: |
          set -e
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data/ debug/prompts/ || true
          if git diff --cached --quiet; then
            echo "rien à committer"
          else
            git commit -m "📊 Mise à jour automatique des portefeuilles"
            git pull --rebase --autostash
            git push
          fi

