name: Génération des Portefeuilles v4 + Backtest

on:
  push:
    branches: [ main ]
    paths:
      - actualites.html
      - marche.html
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-portfolios:
    runs-on: ubuntu-latest
    env:
      API_CHAT: ${{ secrets.API_CHAT }}
      TWELVE_DATA_API: ${{ secrets.TWELVE_DATA_API }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy scipy requests beautifulsoup4 lxml openai pyyaml

      - name: 🚀 Exécuter le moteur v4 + Backtest
        run: python generate_portfolios_v4.py

      - name: 🧾 Check outputs
        if: always()
        run: |
          echo "== data =="
          ls -lah data || true
          echo "----"
          
          # Portfolios
          if [ -f data/portfolios.json ]; then
            echo "✅ portfolios.json"
            sha1sum data/portfolios.json || true
          else
            echo "❌ portfolios.json ABSENT"
          fi
          
          # Backtest results
          if [ -f data/backtest_results.json ]; then
            echo "✅ backtest_results.json"
            echo "Comparaison des 3 profils:"
            python3 << 'EOF'
          import json
          with open("data/backtest_results.json") as f:
              data = json.load(f)
          comp = data.get("comparison", {})
          print(f"{'Métrique':<20} | {'Agressif':>12} | {'Modéré':>12} | {'Stable':>12}")
          print("-" * 65)
          for key in ["total_return_pct", "sharpe_ratio", "max_drawdown_pct"]:
              agg = comp.get("Agressif", {}).get(key, "N/A")
              mod = comp.get("Modéré", {}).get(key, "N/A")
              stb = comp.get("Stable", {}).get(key, "N/A")
              print(f"{key:<20} | {str(agg):>12} | {str(mod):>12} | {str(stb):>12}")
          EOF
          else
            echo "⚠️ backtest_results.json ABSENT (TWELVE_DATA_API non configurée?)"
          fi
          
          echo "----"
          echo "== portfolio_history =="
          ls -lah data/portfolio_history || true

      - name: 📊 Backtest Summary
        if: always()
        run: |
          echo "## 📊 Portfolio Engine v4 Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f data/backtest_results.json ]; then
            echo "### Backtest Comparison (90 days)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Agressif | Modéré | Stable |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|----------|--------|--------|" >> $GITHUB_STEP_SUMMARY
            
            python3 << 'EOF' >> $GITHUB_STEP_SUMMARY
          import json
          with open("data/backtest_results.json") as f:
              data = json.load(f)
          comp = data.get("comparison", {})
          metrics = [
              ("Total Return %", "total_return_pct"),
              ("Sharpe Ratio", "sharpe_ratio"),
              ("Max Drawdown %", "max_drawdown_pct"),
              ("Volatility %", "volatility_pct"),
          ]
          for label, key in metrics:
              agg = comp.get("Agressif", {}).get(key, "N/A")
              mod = comp.get("Modéré", {}).get(key, "N/A")
              stb = comp.get("Stable", {}).get(key, "N/A")
              print(f"| {label} | {agg} | {mod} | {stb} |")
          EOF
          else
            echo "⚠️ Backtest skipped (TWELVE_DATA_API not configured)" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 💾 Commit & push
        if: always()
        run: |
          # Ajouter les artefacts générés
          git add data/portfolios.json \
                  data/backtest_results.json \
                  data/portfolio_history/* || true

          # Si .gitignore les filtre, forcer l'ajout
          if git check-ignore -q data/portfolios.json; then
            git add -f data/portfolios.json
          fi
          if git check-ignore -q data/backtest_results.json; then
            git add -f data/backtest_results.json
          fi
          if git check-ignore -q data/portfolio_history; then
            git add -f data/portfolio_history/*.json || true
          fi

          git -c user.name="github-actions[bot]" \
              -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              commit -m "📊 Portefeuilles v4 + Backtest 90j" || echo "rien à committer"

          # Push simple; si ça échoue pour divergence, tenter un rebase rapide
          git push || (git pull --rebase && git push)
